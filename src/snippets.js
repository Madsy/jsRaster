	/* Get the names of every property in the vertex attribute array as a string.
	 We assume that every propery in "attributes" is a vertex attribute, which is a Vector*/
	/*
	this.attributeNames = function(attribs){
		var arr = [];
		for(a in this.attribs){
			if(!attribs.hasOwnProperty(a)){ continue; }
			arr.push(a);
		}
		return arr;
	}(this.attributes);
	*/
